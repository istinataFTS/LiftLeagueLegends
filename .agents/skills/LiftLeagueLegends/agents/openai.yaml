interface:
  display_name: "LiftLeagueLegends"
  short_description: "Repo-specific patterns and workflows for LiftLeagueLegends"
  default_prompt: "Use the LiftLeagueLegends repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true